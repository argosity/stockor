\begin{document}
<%= partial( 'header', {
  location: @invoice.location, date: @invoice.invoice_date,
  form_title: "Invoice # #{@invoice.visible_id}"
} ) %>
<%= partial 'bill_to_ship_to', record: @invoice -%>
<%= partial 'sku_lines_table', record: @invoice, lines: @invoice.lines.regular -%>
\end{document}
