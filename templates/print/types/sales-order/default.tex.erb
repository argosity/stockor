% <% so = @sales_order %>
\begin{document}
<%= partial( 'header', {
  location: so.location, date: so.order_date, form_title: "Sales Order # #{so.visible_id}"
} ) %>
<%=partial 'bill_to_ship_to', record: so -%>
<%=partial 'sku_lines_table', record: so, lines: so.lines -%>
\end{document}
