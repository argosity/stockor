\rhead{
 \renewcommand\arraystretch{1.3}
 \raisebox{.8\height}{
 \begin{tabular} {rc}
   \hline
   \multicolumn{2}{|c|}{\textbf{\large Invoice \# <%= @invoice.visible_id %>}} \\
       \hline
       Date & <%=q @invoice.created_at.strftime("%b #{@invoice.created_at.day.ordinalize}, %Y") %> \\
       \hline
       Page & \thepage{} of \pageref{LastPage} \\
 \end{tabular}
   }
}
