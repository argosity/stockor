\lhead{
\begin{tabular} {ll}
% <% if @location.logo.present? %>
\raisebox{-0.5\height}{\includegraphics[height=18mm,width=80mm,keepaspectratio]{<%= @location.logo.file.path %>} }
% <% end %>
\parbox{\columnwidth}{ \small <%= break_lines @location.address.to_s(include: [:email,:phone]) %> }
\end{tabular}
}
